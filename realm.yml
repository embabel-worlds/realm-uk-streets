name: realm-uk-streets
description: |
  Street-level Britain, joined at a place: police-recorded crime, every
  registered property sale since 1995, the House Price Index, ONS median pay,
  Census 2021 ethnicity, schools, food-hygiene ratings, live flood alerts,
  and each place's MP with the seat's latest margin — eleven keyless official
  sources queryable as one graph, anchored on `UkPlace`s you watch at two
  zoom levels (a full postcode for street level, an outcode for the district).
  The official ONS district crime table adds a NATIONAL cross-section: every
  local authority's recorded-crime rate per 1,000 residents, rankable and
  joinable to pay. Cross-source views answer what no single register can:
  CrimeVsIncome, CrimeVsIncomeAcrossDistricts, DistrictCrimeLeague,
  IncomeByParty, SeatsAndMargins, PriceTrendAtMyPlaces, WhereNotToEat,
  PlaceDossier. The Street Lens app (`apps/street-lens.html`) puts it all on
  a map of Britain — a dossier per dot.

  The COMMUNITY axis reads what a place-based data platform copies in — school
  readiness, youth justice first-time entrants, NEET, children in low income,
  under-18 conceptions, childhood obesity, life expectancy (OHID Fingertips,
  with the publisher's own "compared to England" on every row), the claimant
  count and population by age (NOMIS), the Indices of Deprivation 2025, and
  DfE early-years, absence and KS4 results — live, dated, at the authority.
  The ORGANISATION axis makes the unit an organisation rather than a
  statistic: every charity declaring the place as an area of operation and
  its daily register record and event history (Charity Commission), every
  published grant into the district and who received it (360Giving), and the
  register record behind any recipient identifier (Find that Charity). Views:
  PlaceProfile, WhereThisPlaceStandsOut, SmartInsights (ranked against every
  authority in England, with a dial), IndicatorTrendAtPlace,
  IndicatorAcrossEngland, MostDeprivedDistricts2025, CharitiesOperatingHere,
  CharityChangesHere, WhoFundsThisPlace, FundedOrganisationsHere,
  SchoolReadinessAtPlace, Ks4AttainmentAtPlace and more. Three DERIVE rule
  sets conclude what no query can: charity succession and lineage from the
  Commission's event log, and money regranted through a publishing
  intermediary from 360Giving. The Community Profile
  app (`apps/community-profile.html`) puts a place's profile on one page.

  No API keys and no accounts — the realm works the moment it is installed.
version: 0.4.0
author: rod
url: https://github.com/embabel-worlds/realm-uk-streets
tags:
  - united-kingdom
  - crime
  - open-data
  - maps
  - apps
exports:
  # the skill — a realm with an explicit export list publishes only the skills it names
  - uk-streets
  - police_uk
  - postcodes_io
  - land_registry
  - nomis
  - fsa
  - ea_floods
  - parliament
  - contracts_finder
  - petitions
  - cqc
  - findthatcharity
  - UkPlace
  - CrimeIncident
  - PropertySale
  - AreaHousePrice
  - AreaIncome
  - EthnicGroupShare
  - FoodPlace
  - FloodAlert
  - Seat
  - SeatResult
  - School
  - Petition
  - ConstituencySignal
  - ContractAward
  - CareLocation
  - DistrictCrime
  - DistrictIncome
  - ConstituencyPay
  - DistrictDensity
  - DistrictYoungAdults
  - DistrictEthnicGroup
  - DistrictDeprivation
  - AreaIndicator
  - ClaimantCount
  - PopulationBand
  - DistrictDeprivation2025
  - CharityAreaLink
  - Charity
  - CharityEvent
  - GrantIntoPlace
  - OrgRecord
  - SchoolReadiness
  - PupilAbsence
  - Ks4Attainment
